# AutoQQ Hermes 业务插件操作指南

> 文档状态：Plugin `0.1.0` 离线实现与 mock/fixture 验证已完成。2026-09-15 已在
> Hermes Agent `v0.21.2` 验证目录插件加载、权限矩阵、delivery worker、QQBot 主动私聊和
> `claim/send/ack` 端到端链路；完整私聊/群聊/@ payload 探针及生产部署仍需按环境复核。
>
> 初始需求来源：仓库根目录 `qqBot.md`。EventServer 已扩展为通用事件平台，Warframe 只是首批事件来源。本项目与 `eventserver` 为两个独立代码项目、独立目录；二者仅通过内部 HTTP API 通信。

## 1. 项目定位

本项目实现 Hermes 的 AutoQQ 业务插件，既负责 QQ 消息进入 LLM 前的确定性处理，也负责从 EventServer 领取待发送通知并通过 Hermes 主动私聊用户：

- 从 Hermes 标准化消息和 QQ 原始事件中读取 `platform`、发送者 `openid`、`chat_id` 与原始 payload。
- 在调用 LLM 或受保护命令前向 EventServer 查询 `chat`、`command` 两个独立权限维度。
- 解析并处理用户命令与管理员命令。
- 已处理命令明确返回 `skip` 或当前 Hermes 版本的等价结果。
- 具有 `chat` 权限的用户，其普通聊天返回 `allow`，继续进入 Hermes 原有会话和 LLM 链路。
- 按命令注册策略执行 `public`、`authorized` 或 `admin` 检查，使安全的命令可配置为无需授权访问。
- 在插件后台任务中定时轮询 EventServer 的待发送队列。
- 以带租约的方式领取待发送消息，防止多个插件实例同时发送同一条任务。
- 调用 Hermes 当前版本验证过的主动发送接口，向目标用户 OpenID 发起 QQ 私聊。
- 将发送成功或失败结果回写 EventServer，由 EventServer 持久化投递状态和安排有限重试。
- 所有用户、角色、订阅和事件状态均通过 EventServer API 操作。

本插件不得：

- 直连 MySQL，或保存一份独立的用户/订阅数据库。
- 轮询任何领域数据源或直接判断事件是否触发；插件只轮询 EventServer 已生成的通用待投递消息。
- 根据昵称、普通 QQ 号或消息文本判定身份。
- 让 LLM 决定权限、订阅、管理员操作或事件触发。
- 自行重复实现 QQ 消息到 LLM、再到 QQ 的完整链路。

## 2. 与 EventServer 的边界

EventServer 是业务数据和权限的唯一事实来源。插件只使用以下内部 API：

| 插件动作 | EventServer API | 说明 |
| --- | --- | --- |
| 查询权限 | `GET /v1/users/{openid}/permission` | 返回账号状态、角色、`chat` 和 `command` 权限 |
| 查看事件 | `GET /v1/events` | 仅展示服务端已注册且启用的事件 |
| 查看订阅 | `GET /v1/users/{openid}/subscriptions` | 对应 `/bindings` |
| 新增订阅 | `POST /v1/users/{openid}/subscriptions` | 对应 `/bind <event_key>`，须幂等 |
| 删除订阅 | `DELETE /v1/users/{openid}/subscriptions/{event_key}` | 对应 `/unbind`，须幂等 |
| 授予权限 | `POST /v1/users/{openid}/grant` | body 指定 `chat`、`command` 或两者；仅管理员 |
| 撤销权限 | `POST /v1/users/{openid}/revoke` | body 指定 `chat`、`command` 或两者；仅管理员 |
| 修改角色 | `POST /v1/users/{openid}/role` | 对应 `/admin`、`/unadmin` |
| 生成授权码 | `POST /v1/pairing-codes` | 无法可靠取得被 @ 用户 OpenID 时使用 |
| 批准授权码 | `POST /v1/pairing-codes/{code}/approve` | 仅管理员可调用 |
| 领取待投递消息 | `POST /v1/deliveries/claim` | 使用 worker ID 和租约批量领取 |
| 确认发送成功 | `POST /v1/deliveries/{delivery_id}/ack` | 必须校验领取时返回的租约 Token |
| 回写发送失败 | `POST /v1/deliveries/{delivery_id}/fail` | 上报可重试性和脱敏错误码 |

所有请求必须携带服务间认证信息，例如：

```http
Authorization: Bearer ${INTERNAL_API_TOKEN}
X-Request-ID: <request-id>
```

管理员写操作还应携带从已验证消息上下文取得的操作者 OpenID。EventServer 必须再次校验操作者角色，不能只相信请求体字段。

权限查询建议返回稳定结构：

```json
{
  "platform": "qqbot",
  "openid": "CURRENT_USER_OPENID",
  "account_status": "active",
  "role": "user",
  "permissions": {
    "chat": false,
    "command": true
  }
}
```

不存在用户与显式封禁用户必须可区分。`account_status=blocked` 的优先级最高，即使命令配置为 `public` 也拒绝访问。

## 3. 已实现目录结构

`0.1.0` 已在当前目录创建以下独立项目结构（核心包实际位于项目根包目录，便于
Hermes 目录插件与 wheel 两种加载方式共用）：

```text
hermes_qqbot_plugin/
├── README.md
├── pyproject.toml
├── .env.example
├── config/
│   └── commands.yaml
├── autoqq_business_plugin/
│   ├── plugin.py
│   ├── commands.py
│   ├── command_policy.py
│   ├── identity.py
│   ├── permission_cache.py
│   ├── eventserver_client.py
│   ├── delivery_worker.py
│   └── hermes_sender.py
├── tests/
│   ├── test_processor.py
│   ├── test_eventserver_client.py
│   ├── test_plugin_adapter.py
│   └── test_delivery_worker.py
└── docs/
    └── qq-payload-probe.md
```

### 3.1 从 GitHub / Git 安装

推荐把包安装到 Hermes 使用的同一 Python 环境。这样 Hermes 会通过已声明的
`hermes_agent.plugins` entry point 发现插件：

```bash
python -m pip install \
  "autoqq-business-plugin @ git+https://github.com/fkYang/hermes_qqbot_plugin.git@main"
hermes plugins enable autoqq-business
```

生产部署应把 `main` 替换为发布 tag 或完整 commit SHA，避免分支移动导致不可重复部署。
更新已有 Git 安装时可执行：

```bash
python -m pip install --upgrade --force-reinstall \
  "autoqq-business-plugin @ git+https://github.com/fkYang/hermes_qqbot_plugin.git@main"
```

需要使用 Hermes 原生目录加载时，可以直接克隆整个仓库：

```bash
git clone https://github.com/fkYang/hermes_qqbot_plugin.git \
  ~/.hermes/plugins/autoqq-business
hermes plugins doctor ~/.hermes/plugins/autoqq-business
hermes plugins enable autoqq-business
```

同一部署只选择一种加载方式，避免同名插件被 entry point 与目录扫描重复发现。目录方式不会
替代 Python 依赖管理，必须保证 Hermes 环境中已有 `httpx>=0.28,<1`。启用前还必须注入
`.env.example` 中列出的配置；不得把真实 Token 提交到 Git。

插件入口已按当前上游稳定契约提供 `plugin.yaml`、根 `__init__.py` 和
`register(ctx)`；实际加载仍必须在锁定的 Hermes 部署中通过 plugin doctor 验证。

## 4. 配置约定

`.env.example` 只写占位值，不写真实 Token、OpenID 或数据库口令：

```dotenv
EVENT_SERVER_URL=http://eventserver:8080
INTERNAL_API_TOKEN=replace-with-a-long-random-secret
EVENTSERVER_CONNECT_TIMEOUT_SECONDS=3
EVENTSERVER_READ_TIMEOUT_SECONDS=5
PERMISSION_CACHE_TTL_SECONDS=30
DEFAULT_CHAT_ACCESS=deny
DEFAULT_COMMAND_ACCESS=deny
DELIVERY_POLL_ENABLED=true
DELIVERY_POLL_INTERVAL_SECONDS=5
DELIVERY_CLAIM_BATCH_SIZE=10
DELIVERY_LEASE_SECONDS=60
DELIVERY_WORKER_ID=replace-with-stable-instance-id
DELIVERY_EMPTY_POLL_BACKOFF_MAX_SECONDS=30
DEFAULT_TIMEZONE=Asia/Shanghai
```

说明：

- 插件不需要、也不得配置任何 `MYSQL_*` 或 `DATABASE_URL`。
- `INTERNAL_API_TOKEN` 必须与 EventServer 的配置一致，并通过本地 Secret 或部署平台注入。
- HTTP 客户端必须设置连接超时、读取超时和有限重试；写请求不得盲目重试。
- 权限缓存建议 30～60 秒；授权、禁用或角色变更成功后立即失效相关缓存。
- 命令访问策略必须使用受版本控制的显式白名单；未注册命令默认拒绝且不得进入 LLM。
- `DEFAULT_CHAT_ACCESS` 和 `DEFAULT_COMMAND_ACCESS` 生产环境保持 `deny`。公开访问通过单条命令的 `public` 策略配置，不得把全局默认改成放行。
- `DELIVERY_WORKER_ID` 在同一运行实例内必须稳定，不同插件实例必须不同。
- 领取租约时间必须覆盖正常发送和回写耗时；发送过程中不得让同一 worker 启动重叠轮询。
- 空队列或 EventServer 暂时不可用时使用带抖动的退避，恢复后继续轮询，不得阻塞 Hermes 消息主链路。

## 5. 实施前探针

编码前必须针对实际锁定的 Hermes 与 QQBot Adapter 版本确认以下内容，并把脱敏结果记录到 `docs/qq-payload-probe.md`：

1. 私聊、群聊和群内 @ 消息的事件类型。
2. 标准化消息中 `source.user_id`、`source.chat_id` 和 `raw_message` 的准确字段。
3. 私聊发送者 `user_openid` 与群成员 `member_openid` 的取得方式。
4. “Agent dispatch 前”的插件扩展点及其相对 Hermes 原生认证流程的执行顺序。
5. 插件如何表达“已处理并跳过 LLM”与“继续交给 LLM”。
6. 插件的注册方法、加载目录与生命周期。
7. Hermes 主动发送到 `qqbot:<OPENID>` 的准确命令/API 和 QQ 平台限制。
8. 插件启动后台 worker、在卸载/退出时取消 worker 的标准生命周期接口。

探针样例必须删除 Token、Secret、完整 OpenID 和真实消息内容。未验证这些项目之前，不得凭旧文档猜测函数签名。

## 6. 双维度授权与命令访问策略

### 6.1 用户权限维度

EventServer 为用户保存两个互不隐含的权限：

| 权限 | 控制范围 | 不代表 |
| --- | --- | --- |
| `chat` | 普通非命令消息能否进入 Hermes AIAgent/LLM | 不自动允许受保护命令 |
| `command` | 能否执行策略为 `authorized` 的命令，以及管理事件订阅 | 不自动允许普通聊天进入 LLM |

允许的组合：

| `chat` | `command` | 效果 |
| --- | --- | --- |
| false | false | 只能执行 `public` 命令 |
| false | true | 可执行公开和已授权命令，但不能聊天 |
| true | false | 可聊天，也可执行公开命令，但不能执行受保护命令 |
| true | true | 可聊天并执行受保护命令 |

`role=admin` 不替代权限开关。管理员命令要求账号为 `active`、角色为 `admin` 且 `command=true`。系统初始化管理员应显式授予两个权限。

事件订阅及其通知归属 `command` 维度：撤销用户的 `command` 权限后，不允许新增或修改订阅，EventServer 也不再为该用户创建新的事件 delivery；是否保留原订阅记录由 EventServer 采用“保留但暂停”的方式处理。

### 6.2 命令策略

每条命令在 `config/commands.yaml` 中声明一种访问策略：

| 策略 | 访问条件 |
| --- | --- |
| `public` | 无需 `chat` 或 `command` 授权；未知用户可访问，但显式封禁用户不可访问 |
| `authorized` | 账号 `active` 且 `command=true` |
| `admin` | 账号 `active`、`command=true` 且 `role=admin` |

建议初始配置：

```yaml
commands:
  /help: public
  /events: public
  /whoami: public
  /bind: authorized
  /unbind: authorized
  /bindings: authorized
  /grant: admin
  /revoke: admin
  /admin: admin
  /unadmin: admin
  /permissions: admin
  /userlist: admin
```

安全约束：

- `public` 表示“无需业务授权”，不表示匿名：插件仍提取可信 OpenID，用于封禁检查、限流和脱敏审计。
- 只有无敏感信息、无管理能力的确定性命令可以设为 `public`。
- 启动时校验策略，禁止将 `/grant`、`/revoke`、`/admin`、`/unadmin`、`/permissions`、`/userlist` 配置为 `public`。
- `/whoami` 设为 `public` 时只显示调用者自己的脱敏身份和权限状态。
- 未注册或格式错误的 `/...` 消息返回“未知命令”与 `/help` 提示，不进入 LLM。
- 命令策略变更需要代码审阅、测试和重新加载插件；普通 QQ 用户不能在聊天中修改策略。
- 为识别显式封禁，`public` 命令仍需查询账号状态；EventServer 不可用时默认失败关闭，不把服务故障解释为公开放行。

### 6.3 消息处理顺序

每条入站 QQ 消息按以下顺序处理：

```text
提取可信发送者 OpenID
  -> 判断是否为注册的斜杠命令
  -> 是命令：读取该命令的 public / authorized / admin 策略
       -> public：检查显式封禁与限流后执行
       -> authorized / admin：查询对应动态权限后执行或拒绝
       -> 命令处理完成后始终返回 skip，绝不进入 LLM
  -> 不是命令：查询 chat 权限
       -> chat=true：返回 allow，进入 Hermes AIAgent
       -> chat=false/用户不存在/用户被封禁：拒绝或返回申请方式，不调用 LLM
```

用户命令：

- `/events`
- `/bind <event_key>`
- `/unbind <event_key>`
- `/bindings`
- `/whoami`
- `/help`

管理员命令：

- `/grant <目标> chat|command|all`
- `/grant <pairing_code> chat|command|all`
- `/revoke <目标> chat|command|all`
- `/admin @用户或openid`
- `/unadmin @用户或openid`
- `/permissions @用户或openid`
- `/userlist`

`<目标>` 可以是可靠解析的 @ 用户或明确 OpenID。授权/撤销必须显式指定维度，避免一次操作意外同时开放 LLM 聊天和业务命令。管理员权限必须来自实际消息发送者 OpenID。若真实 QQ payload 无法可靠取得被 @ 用户的 OpenID，必须使用一次性 pairing code 兜底，不得按昵称授权。

授权请求示例：

```json
{
  "permissions": ["command"],
  "operator_openid": "OPENID_OF_ADMIN"
}
```

重复授予已有权限或撤销不存在的权限必须幂等。`/whoami` 应分别展示 `chat`、`command`、账号状态和角色，而不是只显示笼统的“已授权”。

`/admin` 和 `/unadmin` 只修改角色，不隐式修改 `chat` 或 `command`。若要让新管理员实际执行管理员命令，必须另外显式授予 `command` 权限。

## 7. 待发送消息轮询与主动私聊

用户完成 `/bind <event_key>` 后，EventServer 负责通过对应 provider 监控或接收事件。事件发布时，EventServer 为每个当前有效订阅者生成唯一的待投递记录。插件不理解具体事件领域，也不自行推断状态，只消费统一的 DeliveryMessage。

### 7.1 领取协议

插件按配置周期调用领取接口，示意请求如下：

```http
POST /v1/deliveries/claim
Authorization: Bearer ${INTERNAL_API_TOKEN}
Content-Type: application/json

{
  "worker_id": "hermes-instance-01",
  "platform": "qqbot",
  "limit": 10,
  "lease_seconds": 60
}
```

示意响应：

```json
{
  "items": [
    {
      "delivery_id": "opaque-delivery-id",
      "lease_token": "opaque-one-time-lease-token",
      "event_id": "opaque-event-id",
      "event_key": "warframe.cetus.night",
      "target": {
        "platform": "qqbot",
        "openid": "TARGET_USER_OPENID"
      },
      "message": {
        "text": "希图斯已进入夜晚"
      },
      "attempt": 1,
      "created_at": "ISO-8601 UTC"
    }
  ]
}
```

领取必须在 EventServer 的单个数据库事务内完成，状态从 `pending/retry` 原子更新为 `leased`，同时写入 `lease_owner`、`lease_token` 和 `lease_until`。并发插件实例不得领取到同一条有效租约。

### 7.2 发送与结果回写

对每条领取记录，插件按以下顺序处理：

1. 校验 `delivery_id`、目标平台、OpenID 和消息结构；拒绝未知平台、空目标或超限内容。
2. 调用当前 Hermes 版本验证过的主动发送能力，目标必须是该记录指定的 QQ 私聊用户。
3. 发送成功后调用 `POST /v1/deliveries/{delivery_id}/ack`，携带 `lease_token`、发送时间和可用时的平台代理消息 ID。
4. 发送失败后调用 `POST /v1/deliveries/{delivery_id}/fail`，携带 `lease_token`、脱敏错误码和 `retryable`；是否以及何时重试由 EventServer 决定。
5. 一条消息的处理异常不得终止整个 worker，也不得阻塞 QQ 入站消息处理。

通知发送属于确定性投递，不进入 LLM、不创建普通 Agent 对话轮次。插件应优先使用 Hermes 稳定的进程内发送 API；若锁定版本只能通过其他入口发送，必须先完成兼容性探针并封装在 `hermes_sender.py`，不能把调用细节散落在 worker 中。

### 7.3 可靠性约束

- 投递语义为至少一次：领取后崩溃或发送成功但回写前崩溃，都可能导致租约到期后重试。
- 若 Hermes/QQ 接口支持幂等键，必须使用稳定的 `delivery_id` 作为幂等键；若不支持，应明确记录极小概率重复通知的限制。
- `ack`、`fail` 必须携带当前租约 Token；过期或不匹配的租约不得修改投递结果。
- worker 不自行无限重试。失败次数、下一次尝试时间和最终失败状态由 EventServer 持久化控制。
- 插件优雅退出时停止领取新任务，并在有限时间内完成或回写已领取任务；不能完成的任务交由租约超时恢复。
- EventServer 不可用时不得降级为直接查询 MySQL，也不得绕过服务认证。
- Plugin 不得导入 EventServer 的 Warframe 或其他领域 provider 模型；新事件接入不得要求修改 Plugin。

## 8. 实施状态

1. [x] 锁定并记录实际部署的 Hermes、QQBot Adapter 版本；已验证 Hermes `v0.21.2`。
2. [ ] 完成真实最小 QQ payload 探针；脱敏记录模板已创建。
3. [x] 建立独立插件包、配置加载和脱敏审计日志。
4. [x] 实现带超时、服务认证、响应上限和统一错误处理的 EventServer 客户端。
5. [x] 实现发送者身份提取、双维度权限响应和 LLM 前置门禁。
6. [x] 实现命令注册表、三类策略校验及启动时安全检查。
7. [x] 实现确定性用户命令；所有斜杠命令均不会落入 LLM。
8. [x] 实现显式维度管理员命令和 pairing code；mention 在无可信字段时安全拒绝。
9. [x] 实现短期权限缓存及管理变更后的目标缓存失效。
10. [x] 使用 QQBot platform-connect factory 实现 delivery worker 启动、单线程轮询和停止。
11. [x] 封装 Hermes adapter 主动私聊，并实现租约 `ack`/`fail` 回写。
12. [x] 完成 41 项离线 mock/fixture 测试；EventServer 并发租约语义由服务端套件覆盖。
13. [x] 在锁定测试环境加载插件，完成权限探针与主动私聊端到端验收。

`/userlist` 对应的 EventServer v1 用户列表接口尚不存在；当前命令返回确定性的暂不支持
提示并 `skip`。补齐该能力需要先做版本化跨项目契约变更。

## 9. 验证命令

以下离线命令已使用现有 EventServer Python 3.12 虚拟环境验证；未安装新依赖，也未连接
真实 EventServer、数据库、Hermes、QQ 或外部 API：

```bash
../eventserver/.venv/bin/python -m pytest
../eventserver/.venv/bin/ruff check .
../eventserver/.venv/bin/ruff format --check .
UV_CACHE_DIR=../eventserver/.uv-cache uv build --offline
```

涉及真实 QQ 或 EventServer 的集成测试必须显式启用，默认测试套件不得连接外部服务。

## 10. 验收清单

- [x] 未获得 `chat` 权限的普通消息不会触发 LLM。
- [x] 获得 `chat` 权限的普通消息继续使用 Hermes 原有会话系统，不要求同时具有 `command` 权限。
- [x] 获得 `command` 权限但没有 `chat` 权限的用户可执行受保护命令，但不能触发 LLM。
- [x] 未授权用户可执行配置为 `public` 的命令，显式封禁用户仍被拒绝。
- [x] `public`、`authorized`、`admin` 三类命令策略均有完整权限矩阵测试。
- [x] 管理类命令无法配置为 `public`；未注册斜杠命令不会落入 LLM。
- [x] 所有业务和管理员命令处理后明确跳过 LLM。
- [x] 插件不包含数据库驱动、数据库凭据或直连 MySQL 的代码。
- [x] 管理员操作校验实际发送者 OpenID。
- [x] 无法可靠解析 mention 时使用 pairing code，不按昵称授权。
- [x] EventServer 超时或不可用时返回可理解的错误，不绕过权限检查。
- [x] Token、OpenID 和 pairing code 不以明文写入 Plugin 日志。
- [x] `chat`、`command`、账号状态或角色变更后缓存及时失效。
- [ ] QQ 私聊、群聊、@ 消息均有完整 payload 验证记录；主动发送接口已验证。
- [x] mock/fixture 中，EventServer 待投递消息能被插件轮询领取并经 Hermes adapter 主动私聊。
- [ ] 多个插件实例并发轮询时，同一有效租约只被一个实例领取。
- [x] 发送成功和失败都能按租约回写，失败不会导致 worker 停止。
- [x] Plugin 不持久化租约；重启恢复与有效租约唯一领取由已实现 EventServer 保证。
- [x] 主动通知不进入 LLM，worker 与 QQ 入站消息处理分离。
- [x] 新增符合通用契约的事件类型时无需修改 Plugin 代码。

## 11. 与 EventServer 联调前置条件

开始联调前应由 EventServer 项目提供：

- 可访问的内部基址与 `/health`。
- OpenAPI 或等价的版本化接口文档。
- 测试环境的服务间 Token。
- 固定错误结构：`code`、`message`、`request_id`。
- 分别返回 `chat`、`command`、账号状态和角色的权限接口。
- 支持按权限维度幂等授权和撤销，并在撤销 `command` 后暂停新事件投递。
- 带原子领取、租约超时和并发保护的待投递接口。
- `ack`/`fail` 的租约校验、重试上限和最终失败语义。

接口发生不兼容变更时，必须先升级版本化契约和契约测试，再分别升级两个项目。
